import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<router-outlet></router-outlet>`,
  styleUrls: ['../styles/index.css']
})
export class AppComponent {
  title = 'EPort Healthcare';
}
